# Belle Voice Assistant - Environment Variables
# Copy this file to .env and adjust values as needed.
# All variables are prefixed with BELLE_ and are optional (defaults shown).

# ── Server ───────────────────────────────────────────────────────────────────
BELLE_HOST=0.0.0.0
BELLE_PORT=3002
BELLE_DEBUG=false

# ── Logging ──────────────────────────────────────────────────────────────────
# Log level: DEBUG, INFO, WARNING, ERROR
BELLE_LOG_LEVEL=INFO
# Set to true for structured JSON logging
BELLE_LOG_JSON=false

# ── Smart Home API ───────────────────────────────────────────────────────────
BELLE_SMART_HOME_API_URL=http://localhost:3001/api

# ── Speech-to-Text (Whisper) ─────────────────────────────────────────────────
BELLE_WHISPER_MODEL=mlx-community/whisper-large-v3-mlx
# Leave empty for auto-detect, or set to "en", "pt", etc.
BELLE_WHISPER_LANGUAGE=pt
# 0 = deterministic greedy decoding
BELLE_WHISPER_TEMPERATURE=0.0
BELLE_WHISPER_CONDITION_ON_PREVIOUS=false
# Anti-hallucination thresholds
BELLE_WHISPER_COMPRESSION_RATIO_THRESHOLD=2.4
BELLE_WHISPER_NO_SPEECH_THRESHOLD=0.6
BELLE_WHISPER_LOGPROB_THRESHOLD=-1.0

# ── LLM ──────────────────────────────────────────────────────────────────────
# Provider: local | openai | anthropic
BELLE_LLM_PROVIDER=local
# Local MLX model (used when provider=local)
BELLE_LLM_MODEL=mlx-community/Qwen2.5-14B-Instruct-4bit
BELLE_LLM_MAX_TOKENS=512
BELLE_LLM_TEMPERATURE=0.5

# ── Cloud LLM: OpenAI (requires `uv sync --extra cloud`) ────────────────────
# Required when provider=openai
# BELLE_OPENAI_API_KEY=
BELLE_OPENAI_MODEL=gpt-4o-mini

# ── Cloud LLM: Anthropic (requires `uv sync --extra cloud`) ─────────────────
# Required when provider=anthropic
# BELLE_ANTHROPIC_API_KEY=
BELLE_ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# ── Text-to-Speech (optional, requires `uv sync --extra tts`) ────────────────
BELLE_TTS_ENABLED=false
BELLE_TTS_MODEL=mlx-community/Kokoro-82M-bf16
BELLE_TTS_VOICE=af_heart
BELLE_TTS_SPEED=1.0

# ── Audio ────────────────────────────────────────────────────────────────────
BELLE_SAMPLE_RATE=16000
BELLE_AUDIO_CHANNELS=1
